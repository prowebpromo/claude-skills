# fixes/

One plain-text file per skill: the corrected trigger description from the
2026-07-16 audit, ready to copy-paste. Open the file, select all, copy, and
replace the `description:` value in that skill's SKILL.md frontmatter
(claude.ai -> Settings -> Capabilities -> Skills), keeping the YAML quoting
intact. Each text is verified under the 1024-character limit.
